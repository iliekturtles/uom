[package]
name = "uom-serde"
version = "0.1.0"
edition = "2018"

[dependencies]
uom = { path = "../..", default-features = false, features = ["rational64", "si", "std", "use_serde"]}
serde = { version = "1.0", features = ["derive"] }
